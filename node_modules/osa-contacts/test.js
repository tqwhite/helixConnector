var c = require('./index.js');
var fs = require('fs');

c.getContacts({ name: 'Sarah Tompsidis' }, function(err, data) {
    var sarah = data[0];

    c.getPicture(sarah, false, function(err, data) {
        fs.writeFileSync('sarah.jpg', data);
    });
});
